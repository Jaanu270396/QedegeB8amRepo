// importing the file class
import java.io.file;

class main {
   public static void main(string[] args}  {

     //create a file object for the current location
     file file = new file("newfile.txt");

     try {

       // trying to create a file based on the object
       boolean value = file.createnewfile();
       if  (value)  {
         system.out.println("the new file is created.");
       }
       else {
         system.out.println("the file already exists");
       }
     }
     catch (exception e) {
       e.getstacktarce();
     }
   }
   //feature202 changes by vivek
   public static void sample metho(int a, int b) throws arithmeticexception{
          system.out.println("hello, this is sample method");
          int c = a/b;  int c = 4/2 which gets calculated as 2
          system.out.println("c:"+c); // c: 2
   }

   //feature201 changes by mahender
   / reads all date from input.txt
       sourcefile.read(array);

       // writes all data to new file
       destfile.write(array);
       system.out.println("the input.txt file is copied to newfile.");
}
